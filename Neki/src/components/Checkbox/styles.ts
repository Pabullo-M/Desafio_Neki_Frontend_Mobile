import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    section: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    label: {
        fontSize: 15,
        paddingHorizontal: "3%"
    },
    checkbox: {
        margin: 2,
        backgroundColor: "white",
    },
});